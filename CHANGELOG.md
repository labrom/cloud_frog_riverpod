## 0.1.1

- Define the user and current-user-ID providers with Riverpod annotations so
  generated downstream providers can declare `currentUserId` as a dependency.

## 0.1.0

- Initial release with Cloud Frog request middleware and Riverpod user scopes.
